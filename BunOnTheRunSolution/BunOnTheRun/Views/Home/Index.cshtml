@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BunOnTheRun - Не бігай далеко</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600&family=Unbounded:wght@400;600;700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>

    <div class="main-container">
        <div class="sidebar">
            
            <div class="brand-header">
                <div class="logo-area">
                    <img src="~/images/logo.png" alt="BunOnTheRun Logo" class="site-logo" />
                </div>
                <div class="header-text-group">
                    <h1>Не бігай далеко</h1>
                    <p class="subtitle">Ми десь поряд. Знайди зручну точку,<br />щоб перекусити на ходу.</p>
                </div>
            </div>

            <div class="search-box">
                <label for="cityInput">Введіть адресу:</label>
                
                <div class="input-group">
                    <input type="text" id="cityInput" placeholder="Місто (напр. Київ)" value="Київ" />
                    
                    <div class="address-wrapper" style="position: relative; flex-grow: 1;">
                        <input type="text" id="addressInput" placeholder="Вулиця (напр. Хрещатик)" autocomplete="off" />
                        <ul id="suggestionsList" class="suggestions-list"></ul>
                    </div>

                    <button id="searchBtn" class="search-icon-btn">🔍</button>
                </div>

                <button id="locateBtn" class="btn-location">
                    <span>📍</span> Моя геопозиція
                </button>
            </div>

            <h2 id="resultsTitle">Знайдено пекарень: 0</h2>
            
            <div id="bakeryList" class="bakery-list">
                <div class="empty-state">Введіть адресу для пошуку смаколиків поруч!</div>
            </div>
        </div>

        <div id="map" class="map-container"></div>
    </div>

    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="~/js/site.js?v=5" asp-append-version="true"></script>
</body>
</html>