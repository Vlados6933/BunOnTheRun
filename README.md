# 🥐 BunOnTheRun

**BunOnTheRun** — це веб-сервіс для швидкого пошуку найближчих пекарень у ритмі великого міста. Проєкт поєднує інтерактивну мапу, розумний пошук та деталізовану інформацію про заклади, допомагаючи користувачам знайти свіжу випічку в один клік.

## 👥 Команда розробки (Ctrl+Bun)

* **UI/UX Designer:** Влада Романчук
* **Frontend & DevOps:** Артем Пономаренко
* **Backend Developers:** Влад Безручко, Кіріл Прокопенко
* **QA Engineer:** Артем Проценко

## 🚀 Основні можливості

* **Smart Search:** Пошук пекарень за містом та адресою з автодоповненням (Autocomplete).
* **Geolocation:** Автоматичне визначення місцезнаходження користувача ("Моя геопозиція").
* **Interactive Map:** Відображення закладів на мапі OpenStreetMap з кастомними маркерами.
* **Details & Routing:** Перегляд назви, адреси, графіку роботи та відстані до закладу. Пряме посилання на Google Maps для побудови маршруту.
* **Performance:** Кешування запитів на стороні сервера (In-Memory Cache) для миттєвого пошуку.
* **Localization:** Повна підтримка української мови (інтерфейс та дані карт).

## 🛠 Технологічний стек

### Backend

* **Framework:** .NET 8 (ASP.NET Core MVC & Web API).
* **Services:** `HttpClientFactory`, `IMemoryCache` (для оптимізації зовнішніх запитів).
* **Documentation:** Swagger / OpenAPI.
* **Geospatial Data:**

  * *Photon API (Komoot)* — для прямого геокодингу (координати за адресою).
  * *Overpass API (Kumi Systems)* — для пошуку об'єктів інфраструктури (nodes `shop=bakery`).

### Frontend

* **Markup/Styling:** Razor Views (.cshtml), CSS3 (Custom Grid/Flexbox Layout), адаптивний дизайн.
* **Scripting:** Vanilla JavaScript (ES6+).
* **Maps:** [Leaflet.js](https://leafletjs.com/) — для рендерингу мапи.
* **External APIs:**

  * *Nominatim API* — для зворотного геокодингу та підказок при введенні адреси.
  * *Google Favicons* — для відображення логотипів сайтів пекарень.

## 🏗 Архітектура рішення

Проєкт побудовано за гібридною архітектурою:

1. **MVC Controller (`HomeController`):** Віддає статичну сторінку з інтерфейсом (View).
2. **API Controller (`BakeryController`):** Виступає шлюзом (Gateway) для клієнта.

   * Приймає запит → Перевіряє кеш → Викликає `OsmService`.
   * Обробляє помилки та форматує відповідь.
3. **OsmService:**

   * Виконує запити до зовнішніх гео-сервісів.
   * Парсить "сирі" дані (GeoJSON/JSON) у чисті DTO моделі.
   * Розраховує відстань до кожного об'єкта (Haversine formula).
   * Локалізує графік роботи та назви.

## ⚙️ Налаштування та запуск

### Вимоги

* [.NET 8.0 SDK](https://dotnet.microsoft.com/download)

### Інструкція

1. **Клонування репозиторію:**

```bash
git clone https://github.com/your-username/BunOnTheRun.git
cd BunOnTheRun
```

2. **Відновлення залежностей:**

```bash
dotnet restore
```

3. **Запуск проєкту:**

```bash
dotnet run --project BunOnTheRun
```

4. **Перевірка:**
   Відкрийте браузер за адресою: `http://localhost:5053`

## 📚 API Документація

Після запуску проєкту в режимі розробки (`Development`), доступна Swagger UI документація:
👉 **URL:** `http://localhost:5053/swagger`

### Основний End-point

**`GET /api/bakery/search`**

Пошук пекарень у радіусі 1000 метрів від заданої адреси.

**Параметри:**

* `city` (string, required): Назва міста (напр. "Київ").
* `address` (string, required): Вулиця або район (напр. "Хрещатик").

**Приклад відповіді (JSON):**

```json
{
  "searchCenter": {
    "lat": 50.4501,
    "lon": 30.5234
  },
  "bakeries": [
    {
      "name": "Тітка Клара",
      "latitude": 50.4512,
      "longitude": 30.5240,
      "address": "вулиця Жилянська, 12",
      "openingHours": "Пн-Нд 08:00-21:00",
      "distanceMeters": 150.5
    }
  ],
  "source": "OpenStreetMap"
}
```

## 📂 Структура проєкту

```text
BunOnTheRun/
├── Controllers/
│   ├── BakeryController.cs  # API логіка
│   └── HomeController.cs    # Віддача UI
├── Models/
│   └── OsmModels.cs         # DTO та моделі даних OSM
├── Services/
│   └── OsmService.cs        # Логіка роботи з картами (Photon, Overpass)
├── Views/
│   └── Home/Index.cshtml    # Головна сторінка
├── wwwroot/
│   ├── css/site.css         # Стилі
│   ├── js/site.js           # Логіка фронтенду (Leaflet, Fetch API)
│   └── images/              # Асети (піни, лого)
└── Program.cs               # Конфігурація DI, CORS та Middleware
```

## 📄 Ліцензія

Цей проєкт розповсюджується під ліцензією MIT. Використані дані карт: © [OpenStreetMap contributors](https://www.openstreetmap.org/copyright).
